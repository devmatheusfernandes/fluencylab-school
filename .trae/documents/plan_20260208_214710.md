I will implement sound effects for the placement test and a user setting to toggle them.

### **Backend & Types**
1.  **Update User Type**: Modify `types/users/users.ts` to include a `preferences` object with `soundEffectsEnabled`.
2.  **Update Session Types**: Modify `types/next-auth.d.ts` to expose `preferences` in the `Session` and `JWT` types.
3.  **Update Auth Logic**: Modify `app/api/auth/[...nextauth]/route.ts` to fetch and persist `preferences` in the session token.
4.  **Update Settings API**:
    *   Update `services/core/userService.ts` to handle saving `preferences` in `updateUserSettings`.
    *   Update `app/api/settings/route.ts` to allow validating and passing `soundEffectsEnabled`.

### **Frontend Implementation**
5.  **Create Sound Hook**: Create `hooks/ui/usePlacementSound.ts` to:
    *   Preload sounds (`click`, `correct`, `wrong`, etc.).
    *   Check `session.user.preferences.soundEffectsEnabled` before playing.
    *   Expose methods like `playClick`, `playSuccess`, `playError`.
6.  **Update Settings Page**: Modify `components/settings/SettingsForm.tsx` to add a "Sound Effects" toggle switch that updates the user preference.
7.  **Integrate into Placement Test**: Modify `components/placement/TestView.tsx` to trigger sounds on:
    *   Option selection (`click`)
    *   Answer checking (`success` or `error` based on logic, or just a confirmation sound)
    *   Test completion (`victory`)
    *   Skip (`click` or specific sound)

### **Verification**
*   I will verify that the setting persists after page reload.
*   I will verify that sounds play when enabled and remain silent when disabled.