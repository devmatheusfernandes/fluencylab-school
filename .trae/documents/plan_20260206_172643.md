I will implement the **Mobile Drawer Pattern** for the lesson page, moving the lesson list to a Drawer on mobile devices and adding a trigger button to the sticky header.

### 1. Create `MobileLessonList` Component
I will create a new component `components/course/lesson/MobileLessonList.tsx` specifically for the mobile view.
- **Source**: Adapted from `LessonSidebar`.
- **Changes**:
    - Remove the outer `Card` wrappers to fit seamlessly into the Drawer.
    - Keep the "Study Progress" and "Course Playlist" logic.
    - Ensure responsive styling (e.g., full width, appropriate padding).
    - It will accept the same props: `courseId`, `sections`, `currentLessonId`, `completedLessonIds`, `onMarkComplete`.

### 2. Update Lesson Page (`page.tsx`)
I will modify `app/[locale]/hub/student/my-courses/course/lesson/page.tsx`:
- **State**: Add `const [isDrawerOpen, setIsDrawerOpen] = useState(false)`.
- **Sidebar Visibility**: Add `hidden lg:block` to the desktop `LessonSidebar` container to hide it on mobile.
- **Header Button**:
    - Import `Menu` or `List` icon from `lucide-react`.
    - Add a button in the sticky header (inside the existing flex container) to toggle the drawer.
    - This button will be visible only on mobile (`lg:hidden`).
- **Drawer Implementation**:
    - Import `Drawer`, `DrawerContent`, `DrawerHeader`, `DrawerTitle`.
    - Add the `Drawer` component at the end of the page.
    - Inside the Drawer, render the new `MobileLessonList`.

### 3. Verification
- Verify that the sidebar disappears on mobile and appears on desktop.
- Verify that the new button appears on the header on mobile.
- Verify that clicking the button opens the Drawer with the lesson list and progress.
- Verify that lesson navigation and "Mark Complete" actions work within the Drawer.