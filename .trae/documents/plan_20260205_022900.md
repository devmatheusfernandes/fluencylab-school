# Fix Import Error in Student Profile Page

I will fix the `Cannot find name 'userAdminRepo'` error by updating the imports to use the centralized repository instances from `@/repositories`.

## Steps
1.  **Update Imports**: Modify `app/[locale]/hub/manager/student-profiles/[id]/page.tsx` to import `userAdminRepository` and `planRepository` from `@/repositories`.
2.  **Update Usage**: Replace the usage of `userAdminRepo` with `userAdminRepository`.

This aligns with the project's pattern of using singleton repository instances defined in `repositories/index.ts`.