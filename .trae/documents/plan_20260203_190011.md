I will redesign the **Course Lesson Page** to match your "Dark Mode Video Learning" specifications.

### 1. Data & State Management Updates
- **Update `LessonPageContent` State**: Currently, the page only stores the *current* `lesson`. I will modify it to store the full `sections` array (fetched from API) so we can generate the **Course Playlist** in the sidebar.
- **Progress Calculation**: Calculate percentage and "X of Y" lessons completed on the client side using the `enrollment.progress` map and the total lesson count from `sections`.

### 2. Layout Structure (Responsive Grid)
- Implement a **2-column grid** for Desktop (`grid-cols-1 lg:grid-cols-[1fr_350px]`).
- **Mobile**: Stacked layout (Video -> Content -> Playlist).
- **Theme**: Force specific dark-mode classes (`bg-neutral-900`, `text-neutral-100`) or align with your existing system theme variables if they support the desired "dark aesthetic" natively.

### 3. Left Column: Main Content
- **Hero Video Player**:
  - Extract the **first video block** from the lesson content.
  - Create a wrapper with a 16:9 aspect ratio.
  - **Fallback**: If no video exists, render the requested "Gradient Background Placeholder".
  - **Controls**: Add a visual overlay for controls (Play, Volume, Fullscreen) using Lucide icons.
- **Action Bar**:
  - A distinct `bg-neutral-800/50` row with "Share" and "Bookmark" actions.
- **Course Details (Content & Quiz)**:
  - Render the remaining content blocks (Text, Images, additional Videos).
  - Embed the existing `QuizComponent` at the bottom if the lesson has a quiz.

### 4. Right Column: Sidebar
- **Progress Widget**:
  - Custom "Track" visualization with nodes at 0%, 25%, 50%, 75%, 100%.
  - Motivation text based on progress range.
- **Course Playlist**:
  - Scrollable list of sections and lessons.
  - **Visual States**:
    - **Active**: Highlighted border, "Playing" icon.
    - **Completed**: Green checkmark, dimmed opacity.
    - **Locked/Pending**: Grayed out, Lock/Play icon.

### 5. Components Refactoring
- **Refactor**: Break down the large `page.tsx` into smaller, manageable sub-components:
  - `components/course/lesson/VideoPlayer.tsx`
  - `components/course/lesson/LessonSidebar.tsx`
  - `components/course/lesson/LessonContent.tsx`

### Implementation Plan
1.  **Fetch & Store Sections**: Update `page.tsx` to keep `sections` in state.
2.  **Create Sub-components**: Build the Video Player, Sidebar (Playlist + Progress), and Content components.
3.  **Assemble Layout**: Replace the current `Container` layout with the new Grid layout.
4.  **Styling**: Apply the "Pixel Perfect" Tailwind classes for the dark/modern look.
