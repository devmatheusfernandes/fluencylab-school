Based on your request, I will enhance the Podcast Generation prompt in `actions/lessonProcessing.ts` to leverage the expressive capabilities of the ElevenLabs v3 model.

### **Enhancement Plan**

1.  **Inject Expressiveness Guidelines:**
    I will modify the prompt to explicitly instruct the AI (Gemini) to use specific formatting conventions that the TTS model understands:
    *   **Audio Tags:** Add instructions to use tags like `[laugh]`, `[sigh]`, `[clears throat]` for non-verbal sounds.
    *   **Pacing & Emphasis:** Instruct to use `...` for natural pauses (hesitation, thinking) and `CAPS` for stressed words.
    *   **Emotional Context:** Request explicit emotional cues at the start of sentences where appropriate (e.g., "*(excited)* Wow, really?").

2.  **Character Specifics:**
    *   **Teacher (Sarah):** Instruct to use a warm, encouraging tone, with deliberate pacing for explanations. Use pauses (`...`) to let information sink in.
    *   **Student (Leo):** Instruct to be more energetic, perhaps slightly faster, using hesitation markers (`um...`, `er...`) when "thinking" about a question.

3.  **Refined Prompt Structure:**
    The new prompt will look something like this (conceptual):

    ```text
    ...
    DIRETRIZES DE ÁUDIO (ElevenLabs v3 Optimization):
    - Use '...' para pausas naturais (ex: "Deixe-me pensar... ah, sim!").
    - Use TAGS DE EMOÇÃO entre colchetes para sons não verbais: [laugh], [sigh], [clears throat].
    - Use LETRAS MAIÚSCULAS para palavras enfatizadas (ex: "Isso é MUITO importante").
    - Varie o ritmo: Teacher deve ser calma e articulada; Student deve ser mais espontâneo.
    ...
    ```

### **File to Modify**
*   [lessonProcessing.ts](file:///c:/Users/Mathe/OneDrive/Documentos/Projetos%20de%20Programação/Em%20progresso/fluencylab-school/actions/lessonProcessing.ts): I will replace the existing prompt string with the enhanced version.

This update requires no changes to the logic or other files, as the ElevenLabs service simply synthesizes whatever text string is passed to it. By enriching the input text, we directly improve the audio output quality.