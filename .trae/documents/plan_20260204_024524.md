I will implement the following changes to enhance podcast management and the student viewing experience:

### **1. Data Model Updates**

* **Update** **`Podcast`** **Interface**: Add new fields to `types/learning/immersion.ts`:

  * `coverImageUrl` (string, optional): To store the podcast cover.

  * `language` (string, mandatory): E.g., 'en', 'es', 'fr' 'pt'.

  * `level` (string, optional): E.g., 'A1', 'B2', 'C1'.

### **2. Podcast Management (Admin)**

* **Update** **`PodcastForm.tsx`**:

  * Add an **Image Upload** field for the cover (reusing the blog cover upload logic or creating a new endpoint if needed).

  * Add a **Select/Combobox** for Language (Mandatory).

  * Add a **Select/Combobox** for Level (Optional).

  * Update the `handleSubmit` function to include these new fields when creating/updating.

### **3. Student Podcast List**

* **Update** **`PodcastListClient.tsx`**:

  * **UI Changes**:

    * Display the **Cover Image** on the podcast card (top section).

    * Show **Badges** for Language and Level on the card.

  * **Filtering & Search**:

    * Add a **Search Input** (by title/description).

    * Add a **Language Filter** (Dropdown).

    * Add a **Level Filter** (Dropdown).

    * Implement client-side filtering logic to update the displayed list based on these inputs.

### **4. Backend/API**

* Ensure the upload API supports podcast covers (likely can reuse existing image uploaders).

I will start by updating the type definitions, then the form, and finally the student list view.
