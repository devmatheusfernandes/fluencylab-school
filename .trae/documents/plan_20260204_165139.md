I will update the blog management system to support categories and view tracking, enabling the "Popular Blogs" feature.

### 1. Database & Types
**File:** `types/learning/immersion.ts`
- Update `Blog` interface:
  - Add `categories: string[]`
  - Add `views: number`
- Update `CreateBlogDTO` and `UpdateBlogDTO` to include `categories`.

### 2. Backend Logic (Repository & Service)
**File:** `repositories/learning/immersionRepository.ts`
- **Create**: Initialize `views: 0` and save `categories`.
- **Update**: Allow updating `categories`.
- **New Method**: `incrementBlogViews(id: string)`:
  - Use Firestore `FieldValue.increment(1)` for atomic updates.
- **New Method**: `findPopularBlogs(limit: number)`:
  - Query blogs ordered by `views` (descending).

**File:** `services/learning/immersionService.ts`
- Expose the new repository methods (`incrementBlogViews`, `getPopularBlogs`).
- Pass `categories` through create/update methods.

### 3. Frontend: Blog Editor (Material Manager)
**File:** `components/immersion/BlogForm.tsx`
- Add state for `categories` (selected array).
- Add a "Categories" selection section using Checkboxes (best for the small set of 4 predefined tags).
  - Options: "Productivity", "Development", "UI/UX", "Tutorials".
- Update form submission to include selected categories.

### 4. Verification
- I will verify the changes by creating a new blog post with categories via the updated form.
- I will check if the new fields (`categories`, `views`) are correctly persisted in the mock/database logic.
