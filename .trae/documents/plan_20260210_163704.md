I will implement the **MEI Fiscal Module** updates by extending the transaction system and creating a new dashboard view.

### 1. Data Modeling & Types

* **Update** **`FinanceTransaction`** **Type**:

  * Add `deductible: boolean` (required).

  * Add `fiscalTag?: 'MEI_DEDUCTIBLE' | 'NON_DEDUCTIBLE'`.

* **Update API (`app/api/admin/finance/transactions/route.ts`)**:

  * Modify the POST handler to accept and save `deductible` and `fiscalTag` fields to Firestore.

### 2. Transaction Management UI (`hub/admin/finances/page.tsx`)

* **Update Category List**: Replace/Extend the existing categories with the strict MEI list provided (e.g., "Pagamento de professores", "Softwares e plataformas", "Pró-labore", etc.).

* **Enhance Transaction Form**:

  * Add a **"Dedutível para IRPF?"** toggle/switch.

  * **Auto-selection Logic**: When a category is selected, automatically set the `deductible` state based on the rules (e.g., "Aluguel" -> true, "Pró-labore" -> false).

  * Pass the `deductible` value to the API payload.

### 3. Fiscal Dashboard Logic (`hub/admin/finances/fiscal/page.tsx`)

* **New Calculation Hook**: Create a logic (likely inside a new `useFiscalCalculations`) to:

  * Fetch all `financeTransactions` for the selected year (filtering by date range).

  * **Calculate Metrics**:

    1. **Annual Revenue**: Sum of all `income` transactions.
    2. **Exempt Portion (Isenta)**: Annual Revenue \* 0.32 (32%).
    3. **Accounting Profit (Lucro Contábil)**: (Total Income) - (Total Deductible Expenses).
    4. **Taxable Profit (Lucro Tributável)**: (Accounting Profit) - (Exempt Portion). *If negative, set to 0.*

* **UI Implementation**:

  * Add a new section with **4 Cards** displaying the calculated values.

  * Ensure formatting is consistent (Currency BRL).

### 4. Implementation Steps

1. Modify `app/[locale]/hub/admin/finances/page.tsx` to update types, categories, and form logic.
2. Update \`app/api/

